import React from 'react';

export function AdminSettingsPage() {
  return (
    <div>
      <h1 className="admin-page-title">⚙️ Paramètres</h1>
      <p style={{ color: 'var(--text-muted)' }}>Page des paramètres (À implémenter)</p>
    </div>
  );
}
